val name = "World"
val num = 1
val zero = 0

println(s"${zero + num}: Hello ${name}!")

val text = s"""${num + 1}: Object-Oriented Meets Functional
              |Have the best of both worlds. Construct elegant class
              |hierarchies for maximum code reuse and extensibility,
              |implement their behavior using higher-order functions.
              |Or anything in-between.""".stripMargin

println(text)
